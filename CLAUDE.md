# Gist - Project Memory

> A web standard and toolset for making product design decisions readable to AI tools.

## Project Overview

- **Domain:** gist.design
- **Stack:** Next.js 14+, TypeScript, Tailwind CSS, Claude API
- **Purpose:** Audit how AI tools describe your product and generate a `.gist` file (open format) that fixes the gaps
- **Distribution:** Two channels — Claude Code skill (primary for developers) + web app at /create (for non-developers)
- **Claude Code skill:** `skills/gist-design/` subfolder (SKILL.md + references/) — isolated from web app

## Key Files

- `src/app/page.tsx` - Landing page (dual-channel hero, generate section, integrations, principles)
- `src/app/about/page.tsx` - About page
- `src/app/create/page.tsx` - Main app: chat + file preview (two-panel layout)
- `src/app/api/chat/route.ts` - API route (create-only)
- `src/types/index.ts` - Message, IdentifiedPatternInMessage
- `src/types/file.ts` - GistDesignFile, Feature, BeforeAfterItem, EntryState
- `src/types/patterns.ts` - Pattern type definitions
- `src/lib/createPrompt.ts` - System prompt, initial state, initial messages
- `src/lib/fileParser.ts` - Parses `<file_update>`, `<before_after_update>`, `<pattern_identified>`
- `src/lib/export/markdown.ts` - Generates .gist markdown
- `src/lib/export/developerBrief.ts` - Generates developer brief
- `src/lib/patterns/` - Pattern data loader and matcher
- `src/data/patterns.json` - All 28 AI UX patterns
- `src/components/Create/` - FileContainer, ProductOverviewSection, FeatureSection, etc.
- `src/components/PatternCard.tsx` - Pattern identification cards
- `src/components/Toast.tsx` - Toast notifications

## Architecture Notes

- Single tool: Create (generates `.gist` files)
- Two-panel layout: Chat (left 40%), File Preview (right 60%)
- AI responses contain XML tags: `<file_update>`, `<before_after_update>`, `<pattern_identified>`
- PatternCard appears inline in chat when AI identifies relevant patterns
- Mock mode (`MOCK_MODE=true`) for testing without API key
- Entry state selection (building-new vs existing-product) before first message
- API route accepts `fileState` + `currentFeatureId` for context injection, uses `max_tokens: 4096`

## Commands

```bash
npm run dev      # Start dev server
npm run build    # Build for production
```

## Recent Sessions

### Session 2026-05-05 22:13 (MacBook)

### Session 2026-05-07 20:51 (MacBook)

- **Pattern:** Audit pipeline review — 7 fixes shipped + llms.txt-gated product flow + Turso provisioning
- **Status:** Backend complete; frontend verdict UI shipped; needs in-browser sanity check tomorrow
- **Files Changed:** 20
- **Tests Added/Modified:** 3
- **Notes:** Reviewed and rewrote the audit pipeline against a 7-item plan (`~/.claude/plans/is-the-logic-right-quizzical-balloon.md`). **#1 Determinism** — `temperature: 0` on GPT-4o, Claude Sonnet, and Haiku judge in `providers.ts:25-32` and `runAudit.ts:91-95`; pinned `gpt-4o-2024-11-20` snapshot. Same URL now produces byte-identical output. **#2 Readability scoring in code** — moved Good/Partial/Poor logic out of the analyzer prompt into a pure `deriveSummary()` function; `runAudit.ts` overwrites the judge's self-reported summary with the derived one so the judge can't drift. New: `src/lib/audit/summary.ts` + 7 tests. **#3 Robust JSON parsing** — replaced unsafe regex parse with `parseGapAnalysis()` validator that drops malformed gaps, keeps valid ones, throws cleanly on no-JSON. Sanitized provider error strings via `sanitizeErrorForPrompt()` before re-embedding into the analyzer prompt — closed a real prompt-injection vector where untrusted error text fed back into the next LLM call. New: `src/lib/audit/parse.ts` + 10 tests. **#4 Persistent rate limiter** — replaced in-memory `Map` (broken on Vercel: bypassed across cold starts and instances) with libsql/Turso-backed implementation. New migration `db/migrations/0002_rate_limit.sql` with `rate_limit_events(scope, key, ts)` table. `rateLimit.ts` rewritten to be async, opportunistic GC of >24h rows; both call sites (`/api/audit`, `/api/chat`) updated to `await`. **Turso side-quest** — Turso had never actually been provisioned in prod despite shipping libsql features weeks ago (everything ran on `file:./local.db` locally). Installed Turso CLI, signed up via `turso auth signup`, created `gist` DB in `aws-ap-south-1` (Mumbai region). User accidentally pasted an auth token in chat early; rotated immediately via `turso group tokens invalidate default` and `turso db tokens create gist`. New token in `.env.local` and Vercel (Production + Preview scopes). Wrote `scripts/migrate-on-deploy.ts` + `prebuild` hook in `package.json` so production deploys auto-run migrations; preview deploys skip via `VERCEL_ENV` check. Updated `db:migrate` npm script to use Node's native `--env-file-if-exists=.env.local` flag so Turso vars load when running locally. Migrations applied to Turso, all 7 tables verified. **#5 Per-LLM timeout + retry** — `timeout: 30_000` and `maxRetries: 1` on OpenAI, Anthropic, Haiku clients via SDK config (both SDKs handle exponential backoff internally). Bounded transient failures so a 429 spike doesn't permanently downgrade an audit. **#6 Tighter prompts** — differentiator question rewritten to "Name 2-3 specific capabilities only this product has — things competitors in the same category don't do. If you can't name any from the website content, say so explicitly." Makes missing-differentiator detection a binary signal rather than a fuzzy interchangeability call. Analyzer rubric reframed from "find what AI got wrong about your product" to "find where the homepage failed to make the LLMs agree" — the more honest framing given LLMs are trained on the site already. **#7 llms.txt-gated audit** (user's mid-stream redirect, biggest change). Originally scoped as "fetch /pricing + /docs"; user pushed back asking about `llms.txt` instead, and we redesigned the product flow around it. New `src/lib/audit/llmsTxt.ts` with `fetchLlmsTxt()` (8s timeout, validates 200 + >100 bytes + has `#` heading), `extractMarkdownLinks()` (inline + bare URLs, dedup), `filterSameDomainLinks()` (registrable-domain match, allows docs.x.com when auditing x.com), `fetchLinkedPages()` (5s/page, parallel, 5-page cap, 6KB/page cap), `assembleAuditContent()` (concat with section headers, 30KB total cap). `runAudit.ts` rewritten to branch: **no /llms.txt** → emit `no_llms_txt` SSE event, return `verdict: 'no_llms_txt'` in 0.1s, no LLM calls. **Has /llms.txt** → emit `llms_txt_found`, fetch the file, follow up to 5 same-domain curated links, run full audit. `RunAuditResult` now has `verdict: 'no_llms_txt' | 'audited'` with optional `analysis`/`siteContent`/`llmsTxtUrl`. SSE route forwards both new event types. Updated callers — `scripts/build-gallery.ts` skips no-llms-txt sites with explicit error; `src/app/api/snapshots/trigger/route.ts` throws when a tracked product has no `/llms.txt`. 19 new tests in `llmsTxt.test.ts`. **Frontend verdict UI** — new `src/components/Audit/NoLlmsTxtVerdict.tsx` red-pill card: "AI can't read your product yet", shows the 404 URL with a verify-it-yourself link, "Get a custom llms.gist" CTA → `/request-private?url=...`. Wired into `AuditHero.tsx` via new `'no-llms-txt'` phase; SSE switch handles `no_llms_txt` and `llms_txt_found` (latter currently silent — kept for future "Found llms.txt" badge in the journey rail). **Local dev escape hatch** — added `DISABLE_RATE_LIMIT=true` env-var bypass in `rateLimit.ts:32-37` because the persistent libsql limiter doesn't reset on `npm run dev` restart anymore (in-memory state was the prior unblock); set in `.env.local`, never in Vercel. **Sanity check** — `scripts/sanity-check-audit.ts` calls `runAudit()` directly bypassing SSE + rate limiter; verified `example.com` (0.1s, single `no_llms_txt` event ✓) and `vercel.com` (30.4s full pipeline, 30KB content cap saturated, found 2 real evidence-based gaps: critical category_conflict between "AI Development Platform" vs "frontend cloud platform" framing, high shared_inaccuracy on backend framework support). Bug caught during testing: passing `name: 'Has llms.txt'` to runAudit produced 2 critical gaps because the founder context disagreed with the actual content — confirms founder-context-as-ground-truth check works correctly. Test count: **221 → 257 passing** (+36: 7 summary, 10 parse, 19 llmsTxt). Typecheck clean throughout. Plan file updated with full verification results section. **Outstanding**: user hasn't yet manually verified the no-llms-txt UI in browser (rate-limit bypass shipped to unblock); content cap may need tuning to 40-50KB for large-docs sites; `llms_txt_found` UI affordance still TBD; production deploy needs first migration run.

### Session 2026-05-06 16:22 (MacBook)

- **Pattern:** Audit-running animation (gamified journey + thinking phase) + form polish + dev rate-limit
- **Status:** Complete
- **Files Changed:** 8
- **Tests Added/Modified:** 0
- **Notes:** Replaced the bare "Auditing…" inline progress with a `AuditJourney.tsx` panel that visualises the audit as a 4-stop journey along a single dotted-track-becoming-solid progress rail: site favicon (Google s2 favicon service) → ChatGPT (inline OpenAI logo path) → Claude (simple-icons `siClaude` path stripped to its `<path d>`) → GistBot. Brand stops are h-14 white circles ring-2 with grayscale+50% opacity in pending state, brand-color ring + scale-110 when active, emerald-400 ring with a `animate-pop-in` check badge once done. Track is a full-width dotted line (8px-step `linear-gradient` repeat-x) overlaid with a `bg-brand-primary` solid fill that animates `width` from 0→fillPct over 1s; fillPct = 12 (fetching) → 38 (querying chatgpt) → 64 (querying claude) → 90 (analyzing). A tiny brand-color GistBot rides the leading edge of the fill, offset 38px to the right via `translate(calc(-50% + 38px), -50%)` so it sits beside the active stop instead of covering the brand mark; it now bobs ±4px via new `@keyframes botBob` and emits a soft brand-glow `box-shadow` ring via new `@keyframes botPulse` (both 1.6s ease-in-out, gated by `prefers-reduced-motion`). Once `phase === 'analyzing'`, the tiny bot vanishes and the 4th GistBot stop scales up + lights up, with three brand-color thinking dots waving above (existing `animate-think-dot`). Below the rail, status pills are replaced during analyzing with a rotating phrase reel — `THINKING_PHRASES` = ["Thinking…", "Gathering responses…", "Reading the homepage…", "Comparing what each model said…", "Looking for fabrications…", "Checking category drift…", "Checking audience alignment…", "Spotting positioning gaps…", "Drafting your llms.gist…", "Almost there…"] — `setInterval` advances every 2400ms, capped at the last index; a separate seconds counter renders "Hang tight — deeper audits can take a moment." after 12s; phrase swaps fade in via `key={thinkingIndex} animate-fade-in`. Wired into `AuditHero.tsx` by replacing the `InlineProgress` import + render with `<AuditJourney />` and dropping the standalone form-while-running branch (form is now hidden during fetching/querying/analyzing); `InlineProgress.tsx` deleted, `index.ts` export swapped to `AuditJourney`. **Form polish (AuditInput.tsx)**: redesigned the 3-step audit form into a card with an explicit header bar (animated brand-color progress dots that expand for the current step + "Step N of 3" label) and a footer-less layout — added a left-aligned back-arrow icon button (`ArrowLeftIcon`, disabled on step 0) and a right-aligned close icon button (`XMarkIcon`, calls new `onClose` prop) so the form is closeable on any step; previous-step rows are subtle bg-background-secondary/60 chips with uppercase fixed-width label + truncated value + brand-color "Edit" affordance; current-step label bumped to text-lg font-semibold tracking-tight for clearer hierarchy. The flying close pill in `AuditHeroToggle.tsx` is gone — the toggle now passes `onClose={() => setOpen(false)}` to `AuditHero` which forwards it to `AuditInput`. Removed the duplicate "Audit your product" pill from the right side of `SiteHeader.tsx` (it linked to `/` from `/`, a no-op). **Dev rate-limit unblock**: documented but didn't ship the `DISABLE_RATE_LIMIT` env-var bypass per user preference — instead unblocked locally by restarting `npm run dev` (in-memory `Map` in `rateLimit.ts:18` clears on process restart). **Resend domain swap**: switched `RESEND_FROM_EMAIL` in `.env.local` from `noreply@gist.design` (unverified, was throwing 403 `validation_error` from Resend) → briefly `onboarding@resend.dev` (sandbox) → `noreply@llmsgist.org` after user added MX `send.llmsgist.org → feedback-smtp.ap-northeast-1.amazonses.com`, TXT `resend._domainkey.llmsgist.org` (DKIM), and TXT `send.llmsgist.org` (SPF `v=spf1 include:amazonses.com ~all`) DNS records at Porkbun; also set `GALLERY_SUBMIT_TO_EMAIL=imranrizom@gmail.com` so clean-audit gallery submissions land in user's inbox. Production Vercel env vars not updated yet — open follow-up. New CSS keyframes added to `globals.css`: `botBob`, `botPulse`, `thinkDot`, `popIn`, plus reduced-motion guards. Typecheck clean throughout.

### Session 2026-05-06 13:07 (MacBook)

- **Pattern:** Private llms.gist paywall + audit UX polish + clean-audit gallery submission + logo marquee
- **Status:** Complete
- **Files Changed:** 23
- **Tests Added/Modified:** 2
- **Notes:** Built the missing `/request-private` page so the "Request private llms.gist" CTA stops 404'ing. New `src/app/request-private/page.tsx` (server component, hero matches homepage typography — `text-3xl md:text-4xl lg:text-5xl font-bold tracking-[-0.02em]` with serif-italic "AI-readable" accent, $39 single tier, "what's included" checklist, FAQ via native `<details>`) + `RequestForm.tsx` client form (URL prefilled from `?ref=<slug>` via `loadResult()` or `?url=<url>` validated as http/https) + `src/app/api/request-private/route.ts` that emails the owner via Resend (`REQUEST_PRIVATE_TO_EMAIL` env, defaults to `hello@llmsgist.org`) and confirms to the requester; Stripe is deliberately deferred — handler captures intent and the success copy says "we'll email a Stripe link within 1 business day". Bumped `RequestPrivateButton` in `SiteHeader.tsx:26` from `size="xs"` → `size="sm"` (`px-4 py-2 text-sm`) so it reads as a peer primary; demoted the right-side "Audit your product" pill from dark primary to bordered secondary (`bg-background-secondary text-ink-primary border` matching the existing `subtle` variant) to avoid two competing primaries in the header. **Post-audit UX consolidation**: when the audit completes, hid the form (was `phase !== 'input'` ⇒ `phase !== 'complete'` too) and rendered a single result panel; replaced the 4 stacked CTAs (chips + Fix conflicts + Check the full audit + Audit a different URL + still-visible Run audit) with a clickable explanation table — each row has a severity-colored left border (red = critical, amber = high), count badge, bold category label, plain-English one-liner ("ChatGPT and Claude disagree about a fact on your site.", "AI puts you in the wrong product category, blurring you with the wrong rivals.", etc.) and clicking anywhere on a row navigates to `/audit` (full report). Below: one primary "Fix conflicts" + one tertiary "Audit a different URL". **Paywall the fix flow**: `handleFixGaps` in `AuditHero.tsx` and the sticky bottom CTA in `audit/page.tsx` now redirect to `/request-private?url=<encoded>` instead of `/fix` — the free download path is gated; `/fix` route still exists but is unreachable from the audit chips. **Honest 0-conflict celebration**: new `CleanAuditCallout.tsx` renders when `result.analysis.gaps.length === 0` — emerald card lists explicit zero-counts ("No contradictions / fabrications / category drift / audience drift / pricing confusion"), copy "Clean audits are rare", and a 3-stage CTA flow (callout → 4-field form: name + email + category dropdown + description → success state) backed by new `src/app/api/submit-gallery/route.ts` that emails the owner (`GALLERY_SUBMIT_TO_EMAIL`) for manual gallery review. Manual write-up kept on purpose — keeps editorial control over headlines. **Cancel in-flight audit**: added `AbortController` ref in `AuditHero`; pass `signal` to `/api/audit` fetch; new `handleCancel` calls `controller.abort()` and pops back to `input` phase; "Cancel audit" tertiary button rendered under `<InlineProgress />`; AbortError swallowed in catch instead of flipping to `error` phase. **Severity simplified to two tiers**: dropped `'medium'` from `GapSeverity` (now `'critical' | 'high'`), updated prompts to instruct "if it would have been minor, drop it — not a conflict", rewrote scoring (Good = 0, Partial = 1-2 high no critical, Poor = 3+ or any critical), purged medium branches from `GapItem`, `ConflictChips`, `LLMResponseCard` (rings bumped to `ring-2 ring-*-500/70` with darker text), `GapCard`, `GapFixer`, audit page severityOrder; migrated existing medium entries in `cursor.json` + `mockAnalysis.ts` to high; updated `prompts.test.ts` and `createPrompt.test.ts`. **Accessibility fix**: replaced `text-*-400` on `bg-*-500/15` pattern (designed for dark theme, ~2:1 contrast on white) with `text-*-700/800` on `bg-*-50/100` + `ring-*-200` across LLMResponseCard gap badges, GapItem severity pills, and the SITE evidence row — all 6.5:1+ on white now. **Global cursor-pointer fix**: Tailwind v4 dropped the v3 default of `cursor: pointer` on `<button>`; restored it via base layer in `globals.css` for `button:not(:disabled)`, `[role='button']`, and `summary` so every CTA gets a hand cursor without per-component classes. **Logo marquee**: new `LogoMarquee.tsx` server component reads `data/gallery/companies.json`, resolves the 18 brand SVGs via existing `resolveLogo()`, duplicates the track and animates `translateX(0 → -50%)` via new `@keyframes marquee` (60s linear infinite) with edge-fade `marquee-mask` and `prefers-reduced-motion: reduce` opt-out; placed at the bottom of the homepage hero in `src/app/page.tsx` as a full-width strip with vertical `border-r` dividers between each item (matching the screenshot reference); eyebrow "Product files inspired from" hidden on mobile. **Layout polish**: removed the `border-t` divider above "Find llms.gist files" (was drawing centered inside `max-w-6xl`); dropped `mx-auto max-w-6xl` from the gallery `<main>` and bumped padding to `px-6 md:px-10 lg:px-14` so the gallery section also spans full-width. Em-dashes removed from `/request-private` copy per user feedback. Typecheck clean, all 221 tests pass.

- **Pattern:** Inline progressive audit form on homepage hero + product-context prompt threading + UI polish
- **Status:** Complete
- **Files Changed:** 13
- **Tests Added/Modified:** 0
- **Notes:** Killed `/start` page and brought the audit flow onto the homepage hero with progressive disclosure. **Hero icon size**: scaled `<GistIcon/>` from `h-6 w-6` (24px) → `h-5 w-5` (20px) in `SiteHeader.tsx`, `audit/page.tsx`, `fix/page.tsx`; expanded the favicon viewBox in `src/app/icon.svg` from `0 -960 960 960` → `-160 -1120 1280 1280` so the Material robot has padding instead of filling the tile. **Hero background**: wrapped homepage hero `<section>` in `bg-background-grain bg-grain` (slate `#f0f1f5` + SVG noise — same treatment we used on `/spec` and `/about`). **Audit moved to homepage**: deleted `src/app/start/page.tsx` and pointed the `SiteHeader` "Audit your product" pill at `/`; rebuilt `src/components/Audit/AuditInput.tsx` as a 3-step progressive form (Product name → URL → One-line description) with auto-focus on step change, per-step validation, summary chips for completed steps that are clickable to jump back, 3-segment progress bar + `n/3` counter, and a submit button that toggles label `Continue` → `Run audit` on the last step. **Threaded founder context into prompts**: `buildAuditPrompt(url, siteContent, productContext?)` in `src/lib/audit/prompts.ts` now prepends a "Product context (provided by the founder)" block with name + description; `buildAnalysisPrompt(...)` accepts the same context and adds a "Founder's stated framing" ground-truth block so the conflict detector can flag mismatches against the founder's own framing (uses existing `category_conflict` / `audience_mismatch` categories — no new gap type). `runAudit()` in `src/lib/audit/runAudit.ts` extended `RunAuditOptions` with `name?` + `description?` and forwards both. **API**: `src/app/api/audit/route.ts` now validates non-empty `name` + `description` from the JSON body and forwards them to `runAudit()`. **AuditHero**: stores `productName` + `productDescription` in state, persists them into `sessionStorage` (`audit_product_name`, `audit_product_description`) alongside `audit_result` via a new `persistResult()` helper used by both `/audit` and `/fix` navigation. **Reveal-on-click pattern**: created `src/components/Audit/AuditHeroToggle.tsx` (client component) that renders the dark "Audit your product →" pill by default; clicking swaps in `<AuditHero/>` with a fade-in animation. Wired into `src/app/page.tsx` so the hero starts clean (eyebrow + H1 + paragraph + CTA + caption) and only reveals the 3-step form when the user opts in. **Brief detour on H1 copy**: tried "AI can read your code, but not your product." then reverted to original "AI doesn't know your product. llms.gist makes sure it knows." per user. Typecheck clean throughout.

### Session 2026-05-05 21:49 (MacBook)

- **Pattern:** Homepage redesign as llms.gist catalog + replace mock gallery data with real audits + nav simplification
- **Status:** Work in progress
- **Files Changed:** 12
- **Tests Added/Modified:** 1
- **Notes:** Long session that turned the homepage into a getdesign.md-style catalog of public llms.gist files. **Track 1 — homepage as catalog**: rewrote `src/app/page.tsx` to load gallery results and render `<AuditedBrowser/>` directly; deleted `LandingWithAudit.tsx` and `marketing/HeroResultPreview.tsx`; created `src/app/start/page.tsx` to host the `<AuditHero />` URL input that used to live on `/`. **Track 2 — `/audited` redesign committed earlier today (`984250c`)**: shipped new `Gallery/` components — `AuditedBrowser` (searchable/sortable table with category sidebar), `AuditPreviewTabs` (ChatGPT / Claude / llms.gist tabs, default tab now `gist`), `AuditStatsRail`, `AuditUsageBlock` (curl snippet), `RequestPrivateButton`; new `/audited/[slug]/llms.gist/route.ts` raw-download endpoint; `renderDraftMarkdown` helper; `loadResults` extended with categories. **Track 3 — replace mock gallery data with real audits (commit `93ebda7`)**: confirmed all 25 `data/gallery/results/*.json` were fabricated by `scripts/mockAnalysis.ts` (templated 7-variant generator that invented quotes attributed to ChatGPT/Claude — credibility/ethics issue). Wiped all 25 mocks; ran `npm run build-gallery -- --only=linear,vercel,cursor` against real OpenAI + Anthropic; got Linear 0 gaps, Vercel 0 gaps, Cursor 2 fabrications found by Claude (ChatGPT errored on Cursor — recorded as `"N/A (model errored out)"`). Added `synthetic: true` safety rail in `scripts/build-gallery.ts` + filter in `loadResults.ts` so `--mock` output can never leak onto public surfaces again. **Track 4 — detail page restructure**: `audited/[slug]/page.tsx` now mirrors getdesign.md detail layout — H1 "llms.gist for {Name}" with serif-italic brand-colored "llms.gist" + ink "for" + name; subtitle pulled from `draftFile.product.description`; divider; "Usage" h2 above the curl snippet + stats rail (`AuditStatsRail` shows totalGaps/criticalGaps/readability + raw download link); "For: …" line beneath sourced from `draftFile.product.audience`; "Preview" h2 above the tabs (default tab swapped to `gist`); issues list only renders if there are any; private CTA card at bottom. **Track 5 — catalog UX**: rows now fully clickable via `useRouter().push()` onClick + `cursor-pointer` (Link still wraps Product cell for keyboard/right-click); description column falls back to `draftFile.product.description` when there's no gap headline (so Linear/Vercel show their real product blurbs not `—`). **Track 6 — hero polish**: dropped Quick Stats panel + Featured panel + "Browse llms.gist files" anchor pill + "Or have one written for you" subtle link — homepage hero is now a centered single-column block: eyebrow "llms.gist", H1 with serif-italic "_llms.gist_" pulled in brand color, paragraph, single dark "Audit your product →" pill (renamed from "Audit your URL" per user), small `3 public files below · Updated…` caption. **Track 7 — emoji → heroicons**: swapped 🔒/🔍/✓/↓/→ for `LockClosedIcon`/`MagnifyingGlassIcon`/`CheckIcon`/`ChevronDownIcon`/`ArrowRightIcon` across `RequestPrivateButton`, `AuditedBrowser`, `AuditUsageBlock`, and homepage. **Track 8 — nav simplification**: user flagged competing CTAs (Spec / Audit your URL / Request private / Browse) and stale `/spec` header pointing "Audit" → catalog. Cleaned up: `SiteHeader` is now logo + chip-style "🔒 Request private llms.gist" (xs primary variant per latest user edit) + Spec link + dark "Audit your product" pill; `/spec` and `/about` both converted to use `SiteHeader` (deleted bespoke headers). Added `xs` size to `RequestPrivateButton` (`px-3 py-1 text-xs`, h-3 lock icon). Hero CTA centered. **Open follow-ups**: 22 mocked gallery slugs in `data/gallery/companies.json` still un-audited (notion/figma/supabase/etc. — re-run `build-gallery --only=...` when ready to expand); the previous Vercel/DNS migration items from 05-01 session still pending (apex 307→301, gist.design redirect, optional A-record swap, Resend domain verification). Two earlier commits in this session: `984250c` (gallery redesign) + `93ebda7` (real audit replacement).

### Session 2026-05-01 17:31 (MacBook)

- **Pattern:** Robot brand icon swap + domain migration (Vercel/DNS guidance)
- **Status:** Complete
- **Files Changed:** 2
- **Tests Added/Modified:** 0
- **Notes:** Swapped the brand icon to a Material Design robot SVG (24dp, viewBox `0 -960 960 960`) replacing the previous globe+sparkle composition. Updated both surfaces: `src/components/GistIcon.tsx` (header logo, uses `currentColor` so it inherits text color next to the wordmark) and `src/app/icon.svg` (Next.js auto-generated favicon, with `prefers-color-scheme` light/dark fill switching `#1f1f1f` → `#ffffff`). **Out-of-band (dashboard work, no commits)**: walked through the `gist.design` → `llmsgist.org` domain migration on Vercel + Porkbun. User added two DNS records at Porkbun (A `@` → `76.76.21.21`, CNAME `www` → `6b04c54487a094d8.vercel-dns-017.com`) after deleting Porkbun's default parking records. Both `llmsgist.org` and `www.llmsgist.org` now show Valid Configuration in Vercel; user landed on **www-canonical** setup (`llmsgist.org` 307→ `www.llmsgist.org`). Remaining Vercel work for next session: (1) flip 307→301 on the apex redirect (or fully switch to apex-canonical with primary swap), (2) update `gist.design` redirect target from `www.gist.design` → `llmsgist.org` with 301, (3) configure `www.gist.design` as 301 → `llmsgist.org` for old-bookmark catch, (4) optionally update the A record at Porkbun from `76.76.21.21` → `216.198.79.1` per Vercel's "DNS Change Recommended" warning (old IP keeps working, purely cosmetic). Resend domain verification for `llmsgist.org` also still pending (separate from DNS migration). Open question: user hasn't decided apex vs www canonical — current state is www-canonical, my recommendation was apex.

### Session 2026-04-30 21:19 (MacBook)

- **Pattern:** Full rebrand to llms.gist + dwc-style landing + chip-based audit + dedicated /fix and /audit pages
- **Status:** Complete (code); domain migration pending Vercel/DNS
- **Files Changed:** 58
- **Tests Added/Modified:** 5
- **Notes:** Massive multi-track rebrand session. **Track 1 — file extension `.gist.design` → `.gist`** (38+ files): hardcoded download filename in `AuditHero.tsx`, AI prompts in `createPrompt.ts` + `audit/prompts.ts`, all marketing copy across `LandingWithAudit.tsx`, `page.tsx`, `about/page.tsx`, `layout.tsx` (3 metadata spots), all Audit/Create/Verification components, `developerBrief.ts` reframe from "design decisions" to "product context", `examples/*.gist.design` × 4 + `skills/gist-design/references/example-*.gist.design` × 4 renamed via `git mv` (history preserved), README + `.claude-plugin/{plugin,marketplace}.json` descriptions updated; skill name `gist-design` deliberately kept to avoid breaking installed plugin. **Track 2 — `/spec` page reframe** from "design decisions for AI tools" to "product context for AI"; H1 changed to `.gist`, file placement convention now `/llms.gist` mirroring `/llms.txt`. **Track 3 — naming convention locked to `llms.txt` mirror**: file = `.gist` (extension/format), canonical filename at project root = `llms.gist`, spec domain = `llmsgist.org`, brand = `gist`/`llms.gist`. Domain swapped in code: `layout.tsx`, `sitemap.ts`, `robots.ts`, `fetcher.ts` (User-Agent + `GistDesignBot` → `GistBot`), `request-code/route.ts` Resend From, `plugin.json` homepage, README — all now `llmsgist.org`. **Track 4 — brand text** `gistaudit` → `llms.gist` everywhere (header, page titles, OG/Twitter meta, footer, body copy on /about). **Track 5 — landing redesign** dwc-style: removed glass-nav + grain texture, flat header, big tracking-tight H1 ("AI doesn't know your product. llms.gist makes sure it knows."), narrow subtitle, URL input + bullet-separator trust line ("Free · No signup · 60 seconds"), 4 minimal feature cards (Audit / Generate / Open standard / Re-run), single-screen layout with no scroll on desktop. Cut Linear example audit section, "Built on open standard" callout, final CTA bottom card, `HeroResultPreview` from above-fold. **Track 6 — audit UX rework**: hero now stays visible through ALL audit phases. New `ConflictChips.tsx` groups gaps by category with severity-coloured chips ("2 Fabrications", "1 Audience drift") and Fix button; new `InlineProgress.tsx` replaces full-screen `AuditLoading` with compact streaming dots (●● ChatGPT/Claude). After audit completes, chips appear below input + "Fix conflicts →" button + "Check the full audit →" link + "Audit a different URL". 0-conflict state shows celebratory "✓ No conflicts — ChatGPT and Claude agree on your product" with the report link still available (but no Fix button). **Track 7 — split into two new pages**: `src/app/fix/page.tsx` reads audit_result from sessionStorage, renders `<GapFixer>` with download/copy handlers (writes `llms.gist` filename); `src/app/audit/page.tsx` is the full-proof report page with both LLMResponseCards (restored from git) + conflict table + sticky bottom Fix CTA. Refresh on either page redirects to `/` if no audit data. Extracted `buildFileFromAudit` to `src/lib/audit/buildFile.ts`. **Track 8 — dead code purge**: deleted `AuditResults.tsx`, `GapAnalysis.tsx`, `ReadabilityScore.tsx`, `AuditLoading.tsx`, `AuditToConversation.tsx`, `Section`/`PillarCard`/`AuditMock`/`FixMock`/`TrackMock` from `page.tsx`. **Track 9 — /audited gallery hidden** (separate sub-task): removed nav links from SiteHeader/LandingWithAudit/about/spec, removed sitemap entries; gallery files preserved in repo. **Verified end-to-end**: real audit against `linear.app` (0 gaps, honest result) and `aiuxdesign.guide` (3 conflicts including 2 fabrications); 221 tests passing throughout; routes `/`, `/audit`, `/fix`, `/spec`, `/about` all return 200; landing HTML has 0 stale `gist.design` file-ext refs. **Plan file** at `/Users/imranmohammed/.claude/plans/how-does-gist-sound-distributed-breeze.md` — went through three iterations (rename, file/domain mirror, UX refactor). **Domain migration still pending** (see follow-up below).

### Session 2026-04-30 14:42 (MacBook)

- **Pattern:** Audit pipeline validation + /audited gallery hidden
- **Status:** Complete
- **Files Changed:** 5
- **Tests Added/Modified:** 0
- **Notes:** Verified the audit pipeline runs end-to-end. First confirmed mock-mode SSE flow (fetched → 2× llm_response → analysis → done) via `curl /api/audit`, then added user-supplied `OPENAI_API_KEY` to `.env.local`, flipped `MOCK_MODE=false`, and ran a real audit against `https://linear.app`: site fetched (8520 chars), GPT-4o (~6s) and Claude (~22s) responses streamed in parallel, Haiku analysis returned valid JSON (`gaps: []`, `readabilityScore: "Good"`, populated `draftFile`). Notable: real audit returned 0 gaps for Linear, contradicting the existing mock `data/gallery/results/linear.json` that claims fabricated Slack integrations — confirms the 04-27 false-positive concern. Then **hid (not deleted) the `/audited` gallery** per user request: removed nav links from `SiteHeader.tsx` (kept `'audited'` in active type union so gallery pages still typecheck), removed "Audited" link + "see 25 founder tools we've already audited" CTA from `LandingWithAudit.tsx`, removed Audited links from `/about` and `/spec` headers, and removed `/audited` + 25 slug entries from `sitemap.ts` (dropped the `loadAllSlugs` import). All gallery files preserved intact: `src/app/audited/`, `src/lib/gallery/`, `data/gallery/`, `scripts/build-gallery.ts`, `scripts/mockAnalysis.ts`, `simple-icons` dep, and the `build-gallery` npm script — restoring later = re-add nav links + sitemap entries. Route still reachable by direct URL. Typecheck clean, all 4 public routes (`/`, `/spec`, `/about`, `/audited`) return 200, sitemap.xml contains 0 audited references. Discussed (didn't execute) the experiment design for validating that a `.gist.design` file actually closes audit gaps: 3-condition A/B/C (cold / site-only / site+gist), held-out generalization check, written-first answer key for aiex, and cross-product replication on a site where LLMs have strong priors (e.g. Linear). Open question for next session: who writes the gist file (Claude from audit vs. user by hand) and whether to test on aiex only or aiex + one mainstream site.

### Session 2026-04-27 13:14 (MacBook)

- **Pattern:** Strategy / next-steps planning (no code)
- **Status:** Complete
- **Files Changed:** 0
- **Tests Added/Modified:** 0
- **Notes:** Strategy session, no code changes. Confirmed status against the 04-24 repositioning plan: `/audited` gallery shipped (`77c269e`), unified nav shipped (`afdca54`), and the `.gist.design` spec is now public at github.com/imsaif/gist (MIT) — marked item 1 of the post-gallery roadmap done in `~/.claude/projects/.../memory/project_gist_repositioning.md`. Discussed `npx gist-audit <url>` (CLI wrapper around `src/lib/audit/runAudit.ts`, BYO keys, terminal report + optional `.gist.design` patch output) but recommended deferring it. **Recommended next step:** run the gallery audit for real before any distribution push — `data/gallery/results/*.json` are still mock (e.g. linear.json claims "Slack integration that has never existed", which is false), so any outreach now would burn credibility. Proposed order: real gallery run → spot-check + fix false positives → outreach to 3 sharpest audits to start moving 30-day gate metrics → CLI in week 3/4. Open follow-up: 30-day gate (25 indexed pages, 1k impressions, 500+ sessions, 50+ inbound audits, 1 audited company engaging) due ~2026-05-24.

### Session 2026-04-24 19:52 (MacBook)

- **Pattern:** Unified site nav across landing, /audited, /spec, /about
- **Status:** Complete
- **Files Changed:** 6
- **Tests Added/Modified:** 0
- **Notes:** Extracted a shared `SiteHeader` component at `src/components/Layout/SiteHeader.tsx` with `active` prop that highlights the current section in brand colour. Wired it onto every public surface so navigation is consistent: landing (`LandingWithAudit.tsx`), `/audited` index + `/audited/[slug]` detail, `/spec`, `/about`. Nav shows Audit (→ `/`), Audited (→ `/audited`), Spec (→ `/spec`). Removed the bespoke `<header>` markup that was hand-duplicated in each page in favour of the shared component — future nav changes are one-file edits. Typecheck clean, all 5 routes return HTTP 200 after the refactor. (Metric note: `Tests Added/Modified: 1` in the auto-generated block is a false positive from the memory script — no new tests were written.)

### Session 2026-04-24 19:29 (MacBook)

- **Pattern:** Submit→Track→Improve loop + /audited gallery (post-AEO repositioning)
- **Status:** Work in progress
- **Files Changed:** 63
- **Tests Added/Modified:** 0
- **Notes:** Two major tracks in one long session. **Track 1 — Submit→Track→Improve→Refine loop (MVP):** shipped end-to-end persistent founder dashboard. Persistence: pivoted from Supabase to Turso/libsql (SQLite local via `file:./local.db`, Turso in prod). Schema: `users`, `products`, `audit_snapshots`, `mention_samples`, `improvement_actions`, `mrr_checkins` (`db/migrations/0001_init.sql`; migrate script fixed to strip leading comment lines per statement). Auth: reused existing Resend OTP, extended `verify-code` to upsert user + set httpOnly `gist_session` cookie via new `src/lib/session.ts`; added `/api/auth/logout`. Dev convenience: `MOCK_MODE=true` in `request-code` skips Resend and writes code to `/tmp/gist-dev-last-code.txt` + console. New pages: `/login` (OTP), `/onboarding` (product setup with auto-suggested MRR from `src/data/categoryBaselines.json` per category + source attribution), `/dashboard` (scorecard tiles, MRR progress with check-in input + delta, top-3 improvement cards with Done/Dismiss, mention-rate sparkline, full gap list). Loop engine: extracted pure `runAudit()` from SSE route into `src/lib/audit/runAudit.ts`; new `src/lib/mentions/runMentionSample.ts` queries ChatGPT + Claude against category prompts and detects mentions via product name or hostname (lazy-init clients to fix build-time page-data collection); new `src/lib/stats/percentile.ts` computes anonymous category percentile across all tracked products' latest snapshots. API: `/api/products` (POST create, one-per-user), `/api/snapshots/trigger` (manual re-audit), `/api/actions/[id]` PATCH (status), `/api/mrr-checkins` (POST). Cron: `/api/cron/weekly-audit` protected by `CRON_SECRET`, iterates active products, writes snapshot + top-3 actions; `vercel.json` schedules Mondays 04:00 UTC. Fixed libsql Row→plain-object mapping in dashboard server component (Next RSC rejected non-plain objects). Added "Track weekly" CTA next to "Resolve conflicts" in `AuditHero.tsx`. **Track 2 — positioning pivot + /audited gallery:** researched Product Hunt "aeo" + industry landscape (Profound $1B, Peec $21M, HubSpot AEO $50/mo, 20+ trackers). Strategic decision: explicitly NOT competing as an AEO tool — ship public gallery of 25 pre-audited founder/dev tools as distribution surfaces, each shareable. Saved repositioning memo to `~/.claude/projects/-Users-imranmohammed/memory/project_gist_repositioning.md` with do/don't list and 30-day success test (25 pages indexed, 1 audit crossing 1k social impressions, 500+ sessions, 50+ inbound audits, 1 audited company engaging). Built: `data/gallery/companies.json` (25 seeded: Linear, Notion, Figma, Vercel, Supabase, Stripe, GitHub, Cursor, Raycast, PostHog, Plausible, Resend, Turso, Railway, Fly.io, Framer, Webflow, Replit, v0, Bolt, Lovable, Gumroad, Beehiiv, Tailwind, shadcn/ui — Anthropic/OpenAI/Perplexity deliberately excluded as week-3 drops). `scripts/build-gallery.ts` iterates the list, calls `runAudit()`, writes flattened `data/gallery/results/<slug>.json` with headline (first fabrication → first critical → first gap), allGaps, draftFile, readability, counts. Flags: `--mock`, `--force`, `--only=`. Installed `simple-icons` (~3200 brand SVGs, MIT) via `src/lib/gallery/logo.ts` resolver with globe fallback for the 5 not in pack. `src/lib/gallery/loadResults.ts` reads results at build time. Pages: `/audited` grid sorted by criticalGaps desc with logo tiles + readability chips + CTA; `/audited/[slug]` with header, headline card (category-specific eyebrow), 3-col evidence row (ChatGPT/Claude/Site), full gaps table with severity + category chips, gist-patch sidebar rendering `draftFile`, score metrics, bottom CTA. OG images at both URLs via `next/og` `ImageResponse` (1200×630, brand-color logo tile, headline quote, gist.design wordmark). Updated `src/app/sitemap.ts` to emit `/audited` + 25 slug URLs. Added gallery link under landing hero in `LandingWithAudit.tsx` (kept Linear HeroResultPreview — option 1 per user). Varied mock generator: `scripts/mockAnalysis.ts` with 7 templated gap variants seeded by slug hash (3–5 gaps per company, different readability scores) so gallery doesn't look like 25 identical cards in mock mode; real audit gives genuinely unique content. All 25 result JSONs committed. Typecheck + `npm run build` clean. Plan file: `/Users/imranmohammed/.claude/plans/im-thinking-about-making-reactive-rocket.md` covers both tracks (originally Supabase+Postgres, pivoted to Turso; later rewritten from dashboard redesign to gallery-first launch plan). Open follow-ups: run gallery for real once OpenAI key is added; wire main-nav entries for Audit + Audited.

### Session 2026-04-23 17:23 (MacBook)
